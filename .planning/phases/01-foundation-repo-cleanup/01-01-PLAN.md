---
phase: 01-foundation-repo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - google-apps-script.js
  - index.html
  - postcss.config.js
  - src/App.jsx
  - src/components/ChoiceCard.jsx
  - src/components/ProgressBar.jsx
  - src/components/RatingScale.jsx
  - src/components/SurveyStep.jsx
  - src/components/ThankYou.jsx
  - src/data/questions.js
  - src/hooks/useSurvey.js
  - src/index.css
  - src/main.jsx
  - src/pages/Landing.jsx
  - src/pages/Survey.jsx
  - tailwind.config.js
  - vite.config.js
  - apps/landing/package.json
autonomous: true
requirements: [FOUND-01]
tags: [phase-1, cleanup, gitignore, monorepo-hygiene]
must_haves:
  truths:
    - "git status --porcelain shows zero ` D ` (space-D) entries at root"
    - ".gitignore ignores .DS_Store at root and all nested levels"
    - "apps/landing has no framer-motion in dependencies (unused)"
  artifacts:
    - path: ".gitignore"
      provides: "Ignores .DS_Store, .sentry/, .sentryclirc, coverage/, .vscode/, .idea/"
      contains: ".DS_Store"
    - path: "apps/landing/package.json"
      provides: "Landing package.json without framer-motion"
      must_not_contain: "framer-motion"
  key_links:
    - from: ".gitignore"
      to: "repo root + nested dirs"
      via: "glob pattern **/.DS_Store"
      pattern: "\\*\\*/\\.DS_Store"
---

<objective>
Clean the working tree: commit the 18 pre-monorepo root-file deletions currently staged as ` D ` in `git status`, extend `.gitignore` to cover `.DS_Store` (currently untracked at root and `apps/.DS_Store`), and remove the unused `framer-motion` dependency from `apps/landing`. This is the git-hygiene foundation — every subsequent plan assumes a clean working tree.

Purpose: Satisfy FOUND-01 partial (stale file cleanup) per D-20 and CONTEXT §code_context (`.DS_Store` untracked cleanup).
Output: Two atomic commits — `chore: add .DS_Store and build artifacts to .gitignore` then `chore: remove pre-monorepo root files` then `chore(deps): remove unused framer-motion from landing`. Bisect-friendly sequence per 01-RESEARCH §Git Cleanup Mechanics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-repo-cleanup/01-CONTEXT.md
@.planning/phases/01-foundation-repo-cleanup/01-RESEARCH.md
@.planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
Current `.gitignore` (all 6 lines — read before modifying):
```
node_modules
dist
.env
.env.local
.vercel
.next
```

Current `apps/landing/package.json` dependencies block (line 12-17):
```json
"dependencies": {
  "framer-motion": "^11.0.0",
  "react": "^18.3.1",
  "react-dom": "^18.3.1",
  "react-router-dom": "^7.13.1"
}
```

18 files currently staged as ` D ` in working tree (from `git status --porcelain | grep '^ D'`):
.env.example, google-apps-script.js, index.html, postcss.config.js, src/App.jsx, src/components/ChoiceCard.jsx, src/components/ProgressBar.jsx, src/components/RatingScale.jsx, src/components/SurveyStep.jsx, src/components/ThankYou.jsx, src/data/questions.js, src/hooks/useSurvey.js, src/index.css, src/main.jsx, src/pages/Landing.jsx, src/pages/Survey.jsx, tailwind.config.js, vite.config.js

2 untracked files (from `git status --porcelain | grep '^??'`):
.DS_Store, apps/.DS_Store
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend .gitignore with .DS_Store, build artifacts, and IDE directories</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current 6 lines — confirm starting state before append)
    - .planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md §5 (exact append block; `**/.DS_Store` required because plain `.DS_Store` does not recurse in all git implementations)
    - .planning/phases/01-foundation-repo-cleanup/01-RESEARCH.md §Runtime State Inventory (confirms no committed `.DS_Store` in history — this is additive)
  </read_first>
  <action>
    Append the following block to `.gitignore` (preserve the existing 6 lines: `node_modules`, `dist`, `.env`, `.env.local`, `.vercel`, `.next`). Final file ends with these new sections:

    ```
    
    # OS metadata
    .DS_Store
    **/.DS_Store
    
    # Sentry build artifacts
    .sentry/
    .sentryclirc
    
    # Test output
    coverage/
    
    # IDE
    .vscode/
    .idea/
    ```

    Both `.DS_Store` (root) and `**/.DS_Store` (nested) must be present — git's .gitignore matching for a bare filename without slash does NOT always recurse subdirectories depending on implementation; explicit `**/.DS_Store` is defensive. Per D-20 + CONTEXT §code_context, the 2 untracked `.DS_Store` files (root + `apps/`) will be ignored after this change without needing `git rm` (they were never committed — verified in RESEARCH §Runtime State Inventory).

    Commit this change alone with message: `chore: add .DS_Store and build artifacts to .gitignore`.
  </action>
  <verify>
    <automated>grep -q '^\.DS_Store$' .gitignore && grep -q '^\*\*/\.DS_Store$' .gitignore && grep -q '^\.sentry/$' .gitignore && grep -q '^coverage/$' .gitignore && git status --porcelain | grep -vE '^\?\?\s+(\.planning|\.claude)' | grep '^??' | grep -E '\.DS_Store$' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the literal line `.DS_Store`
    - `.gitignore` contains the literal line `**/.DS_Store`
    - `.gitignore` contains `.sentry/`, `.sentryclirc`, `coverage/`, `.vscode/`, `.idea/`
    - `git status --porcelain` no longer lists `.DS_Store` or `apps/.DS_Store` as `??` untracked
    - The 6 pre-existing lines (`node_modules`, `dist`, `.env`, `.env.local`, `.vercel`, `.next`) are still present
    - A commit exists on the branch with exact message `chore: add .DS_Store and build artifacts to .gitignore`
  </acceptance_criteria>
  <done>
    .gitignore has the appended block; `git status` no longer shows `.DS_Store` as untracked; commit `chore: add .DS_Store and build artifacts to .gitignore` exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the 18 pre-monorepo root-file deletions</name>
  <files>
    .env.example, google-apps-script.js, index.html, postcss.config.js,
    src/App.jsx, src/components/ChoiceCard.jsx, src/components/ProgressBar.jsx,
    src/components/RatingScale.jsx, src/components/SurveyStep.jsx,
    src/components/ThankYou.jsx, src/data/questions.js, src/hooks/useSurvey.js,
    src/index.css, src/main.jsx, src/pages/Landing.jsx, src/pages/Survey.jsx,
    tailwind.config.js, vite.config.js
  </files>
  <read_first>
    - .planning/phases/01-foundation-repo-cleanup/01-CONTEXT.md §decisions D-20 (single commit, single message)
    - .planning/phases/01-foundation-repo-cleanup/01-RESEARCH.md §Git Cleanup Mechanics (full list with count reconciliation: RESEARCH says 18, CONTEXT says 17 — live `git status` is authoritative)
    - .planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md §"Working Tree Cleanup" (per-file delete table)
  </read_first>
  <action>
    Run `git status --porcelain | grep '^ D '` to list the exact files currently staged for deletion. Stage all of them with `git add -u` (this stages only currently-tracked modifications/deletions — it does NOT pick up the `.DS_Store` files since those are handled by Task 1 via `.gitignore`).

    Do NOT use `git add -A` or `git add .` — prefer explicit deletions only, per the user-level CLAUDE.md "staging files" rule.

    Verify the staged set with `git status --porcelain --cached` — must show exactly these 18 deletions and nothing else:
    1. `.env.example`
    2. `google-apps-script.js`
    3. `index.html`
    4. `postcss.config.js`
    5. `src/App.jsx`
    6. `src/components/ChoiceCard.jsx`
    7. `src/components/ProgressBar.jsx`
    8. `src/components/RatingScale.jsx`
    9. `src/components/SurveyStep.jsx`
    10. `src/components/ThankYou.jsx`
    11. `src/data/questions.js`
    12. `src/hooks/useSurvey.js`
    13. `src/index.css`
    14. `src/main.jsx`
    15. `src/pages/Landing.jsx`
    16. `src/pages/Survey.jsx`
    17. `tailwind.config.js`
    18. `vite.config.js`

    If `git status` shows only 17 deletions at execution time (the CONTEXT-vs-RESEARCH discrepancy), proceed with whatever the live count is — this is expected drift.

    Commit with exact message (per D-20 + RESEARCH §Git Cleanup Mechanics step 2): `chore: remove pre-monorepo root files`.
  </action>
  <verify>
    <automated>git status --porcelain | grep -E '^ D ' | wc -l | xargs test 0 -eq && git log -1 --pretty=%s | grep -qE '^chore: remove pre-monorepo root files$'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain | grep '^ D '` returns zero lines
    - Latest commit message exactly `chore: remove pre-monorepo root files`
    - No files under `src/` at repo root exist in `git ls-files` (confirms deletions applied)
    - `apps/` tree is untouched (sanity check — do not accidentally delete from monorepo subtrees)
  </acceptance_criteria>
  <done>
    18 deletions committed. Working tree clean of staged deletions. `git ls-files | grep '^src/'` returns zero lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove unused framer-motion dependency from apps/landing</name>
  <files>apps/landing/package.json, package-lock.json</files>
  <read_first>
    - apps/landing/package.json (lines 12-17 — verify framer-motion is listed)
    - .planning/phases/01-foundation-repo-cleanup/01-RESEARCH.md §Pitfall 9 (MUST grep for imports first; `grep -r "framer-motion\|'motion/react'\|from \"framer" apps/landing/src` must return zero before uninstall)
    - .planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md §Metadata (confirms framer-motion appears ONLY in package.json deps, no imports)
    - .planning/codebase/CONCERNS.md §"Fragment: Tech Debt in Dependencies" (source of claim)
  </read_first>
  <action>
    BEFORE modifying anything, verify framer-motion has no import sites:
    ```bash
    grep -rnE "framer-motion|'motion/react'|from \"framer" apps/landing/src/
    ```
    Expected result: zero matches. If ANY match appears, STOP and raise in execution summary — do not uninstall.

    If grep is clean, run: `npm uninstall framer-motion --workspace=apps/landing`. This updates `apps/landing/package.json` (removes the line `"framer-motion": "^11.0.0"`) and regenerates `package-lock.json` entries.

    After uninstall, run `vite build --workspace=apps/landing` OR `npm run build --workspace=apps/landing` to confirm the build still succeeds (RESEARCH §Pitfall 9 — runtime-side safety check).

    Commit with exact message: `chore(deps): remove unused framer-motion from landing`.
  </action>
  <verify>
    <automated>! grep -q 'framer-motion' apps/landing/package.json && ! grep -rqE "framer-motion|'motion/react'|from \"framer" apps/landing/src/ && npm run build --workspace=apps/landing 2>&1 | tail -5 | grep -q 'built in'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q framer-motion apps/landing/package.json` returns non-zero (string absent)
    - `grep -rn framer-motion apps/landing/src/` returns zero matches
    - `npm run build --workspace=apps/landing` exits 0 and produces `apps/landing/dist/`
    - `package-lock.json` no longer contains framer-motion entries (auto-handled by npm)
    - Latest commit message exactly `chore(deps): remove unused framer-motion from landing`
  </acceptance_criteria>
  <done>
    framer-motion uninstalled; grep clean; landing build succeeds; commit created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git index | deletions must not accidentally remove files outside the 18 listed |
| developer laptop → git history | `.DS_Store` must not leak into future commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01-01 | Tampering | `git add` scope | mitigate | Use `git add -u` (tracked-only updates) NOT `git add -A`/`git add .`; verify staged set with `git status --porcelain --cached` before committing |
| T-1-01-02 | Information Disclosure | `.DS_Store` leakage | mitigate | Both `.DS_Store` and `**/.DS_Store` patterns in .gitignore; RESEARCH §Runtime State Inventory confirms zero committed instances so no history rewrite required |
| T-1-01-03 | Denial of Service | Landing build regression from framer-motion removal | mitigate | Mandatory grep verification before uninstall (RESEARCH §Pitfall 9) + post-uninstall `vite build` smoke check |
</threat_model>

<verification>
- `git status` clean (zero ` D `, zero `??` for `.DS_Store` variants)
- `git log --oneline -3` shows three commits in order: .gitignore → root deletions → framer-motion uninstall
- `apps/landing` still builds
- `apps/app` untouched (`git diff HEAD~3 apps/app/` shows no changes)
</verification>

<success_criteria>
1. Three atomic commits created in stated order
2. Working tree clean of pre-monorepo legacy (`git status --porcelain | grep '^ D '` → 0 lines)
3. `.DS_Store` permanently ignored at all levels
4. framer-motion removed from `apps/landing` deps and lockfile; landing build passes
5. No changes outside scope (no touches to `apps/app/`, `.planning/`, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-cleanup/01-01-SUMMARY.md` documenting:
- Final `git status` (must be clean except potentially for `.planning/` changes)
- `git log --oneline -3` output
- Confirmation that `vite build` landing succeeded post-framer-motion removal
</output>
