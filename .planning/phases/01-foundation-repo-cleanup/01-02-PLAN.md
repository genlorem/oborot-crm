---
phase: 01-foundation-repo-cleanup
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - apps/landing/package.json
  - apps/app/package.json
  - apps/landing/google-apps-script.js
  - .claude/launch.json
  - package-lock.json
autonomous: true
requirements: [FOUND-01]
tags: [phase-1, rebrand, monorepo, package-names]
must_haves:
  truths:
    - "Root package.json name is 'oborot-crm'"
    - "apps/landing package name is '@oborot/landing'"
    - "apps/app package name is '@oborot/app'"
    - "apps/landing/google-apps-script.js header says 'Оборот — Google Apps Script'"
    - "Literal strings 'nashsklad' and 'НашСклад' do not appear in any tracked code file (.js/.jsx/.json/.ts/.tsx) outside .planning/"
    - "package-lock.json regenerated with new names (`@oborot/landing`, `@oborot/app`)"
  artifacts:
    - path: "package.json"
      provides: "Root monorepo identifier"
      contains: "\"name\": \"oborot-crm\""
    - path: "apps/landing/package.json"
      provides: "Landing workspace identifier"
      contains: "\"name\": \"@oborot/landing\""
    - path: "apps/app/package.json"
      provides: "App workspace identifier"
      contains: "\"name\": \"@oborot/app\""
    - path: "apps/landing/google-apps-script.js"
      provides: "Rebranded GAS header"
      contains: "Оборот — Google Apps Script"
  key_links:
    - from: "package-lock.json"
      to: "workspace packages"
      via: "npm install regenerates lockfile with new @oborot/* names after package.json rename"
      pattern: "@oborot/(landing|app)"
---

<objective>
Rebrand all package identifiers and in-code text from "nashsklad"/"НашСклад" to "oborot-crm"/"@oborot"/"Оборот" in a single PR per D-18. This is a mechanical, cross-cutting rename covering 3 package.json files, 1 Google Apps Script header, 1 optional debug config, and a regenerated lockfile.

Purpose: Satisfy FOUND-01 (package rename + text rebrand) per D-17, D-18, D-19. Unblocks every subsequent plan that will install dependencies or reference workspaces — `package-lock.json` must be internally consistent before Wave 3.
Output: Single atomic commit `chore: rebrand nashsklad to oborot` encompassing all name changes + regenerated `package-lock.json`. Bisect-safe: the repo builds both before and after this commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-repo-cleanup/01-CONTEXT.md
@.planning/phases/01-foundation-repo-cleanup/01-RESEARCH.md
@.planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md
@.planning/phases/01-foundation-repo-cleanup/01-01-SUMMARY.md

<interfaces>
Current `package.json` (root, all 12 lines):
```json
{
  "name": "nashsklad",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "landing": "npm run dev --workspace=apps/landing",
    "app": "npm run dev --workspace=apps/app"
  }
}
```

Current `apps/landing/package.json` name field (line 2): `"name": "@nashsklad/landing"`.
Current `apps/app/package.json` name field (line 2): `"name": "@nashsklad/app"`.
Current `apps/landing/google-apps-script.js` line 2: ` * НашСклад — Google Apps Script`.
`.claude/launch.json` line 5: contains `"name": "nashsklad-landing"` (per RESEARCH §Runtime State Inventory + §Open-Q-6).

Target rename matrix (D-17, D-19):
| File | From | To |
|------|------|-----|
| `package.json` root | `"name": "nashsklad"` | `"name": "oborot-crm"` |
| `apps/landing/package.json` | `"@nashsklad/landing"` | `"@oborot/landing"` |
| `apps/app/package.json` | `"@nashsklad/app"` | `"@oborot/app"` |
| `apps/landing/google-apps-script.js` line 2 | `НашСклад — Google Apps Script` | `Оборот — Google Apps Script` |
| `.claude/launch.json` | `"nashsklad-landing"` | `"oborot-landing"` |
| `package-lock.json` | 6+ refs to `@nashsklad/*` | regenerated via `npm install` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package.json name fields across root + both workspaces</name>
  <files>package.json, apps/landing/package.json, apps/app/package.json</files>
  <read_first>
    - package.json (root — line 2 current state)
    - apps/landing/package.json (line 2 current state; framer-motion MUST already be removed by Plan 01 Task 3)
    - apps/app/package.json (line 2 current state)
    - .planning/phases/01-foundation-repo-cleanup/01-CONTEXT.md §D-17 (exact target names: `@oborot/landing`, `@oborot/app`, `oborot-crm`)
    - .planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md §1-§3 (per-file rename patterns, gotchas)
  </read_first>
  <action>
    Apply the three renames exactly — no other fields touched in this task:

    1. `package.json` (root) — change ONLY `"name": "nashsklad"` → `"name": "oborot-crm"`. Leave `workspaces`, `scripts`, etc. unchanged.
    2. `apps/landing/package.json` — change ONLY `"name": "@nashsklad/landing"` → `"name": "@oborot/landing"`.
    3. `apps/app/package.json` — change ONLY `"name": "@nashsklad/app"` → `"name": "@oborot/app"`.

    Do NOT add/remove other fields, do NOT reorder keys, do NOT touch scripts, dependencies, or devDependencies — those are handled in Plans 03–06. This task is pure rename.

    Do NOT run `npm install` yet — the lockfile regen is Task 4 so it captures all related changes at once.
  </action>
  <verify>
    <automated>node -e "process.exit(require('./package.json').name === 'oborot-crm' ? 0 : 1)" && node -e "process.exit(require('./apps/landing/package.json').name === '@oborot/landing' ? 0 : 1)" && node -e "process.exit(require('./apps/app/package.json').name === '@oborot/app' ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').name"` prints exactly `oborot-crm`
    - `node -p "require('./apps/landing/package.json').name"` prints exactly `@oborot/landing`
    - `node -p "require('./apps/app/package.json').name"` prints exactly `@oborot/app`
    - `workspaces` field in root is unchanged (`["apps/*"]`)
    - Root `scripts` unchanged (`landing`, `app` entries intact)
    - `apps/landing/package.json` still has `react-router-dom`, `react`, `react-dom` deps unchanged; framer-motion already absent (Plan 01 Task 3)
    - `apps/app/package.json` `next`, `@supabase/supabase-js`, `zustand`, `@tanstack/react-query` deps unchanged
    - `git diff --stat package.json apps/*/package.json` shows only line-2 change per file
  </acceptance_criteria>
  <done>
    Three package.json name fields updated. No dep/script changes. Lockfile not yet regenerated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand google-apps-script.js header and .claude/launch.json</name>
  <files>apps/landing/google-apps-script.js, .claude/launch.json</files>
  <read_first>
    - apps/landing/google-apps-script.js (lines 1-15 — verify "НашСклад" on line 2)
    - .claude/launch.json (all lines — verify "nashsklad-landing" on line 5)
    - .planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md §4 (google-apps-script header rule: change ONLY line 2, no other code touches)
    - .planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md §28 (`.claude/launch.json` optional rename per D-19 "любые другие вхождения")
  </read_first>
  <action>
    1. `apps/landing/google-apps-script.js` — change line 2 from:
       ` * НашСклад — Google Apps Script`
       to:
       ` * Оборот — Google Apps Script`
       Do NOT modify any other line. This file is deployed separately to Google Apps Script — the rebrand is cosmetic in the header only.

    2. `.claude/launch.json` — change the `"name"` value containing `"nashsklad-landing"` to `"oborot-landing"`. Leave `type`, `request`, `program`, and any other fields untouched.

    After both edits, run `git diff apps/landing/google-apps-script.js .claude/launch.json` and verify the diff is exactly 2 lines (one per file, modifying only the brand-bearing lines).
  </action>
  <verify>
    <automated>grep -q '^ \* Оборот — Google Apps Script$' apps/landing/google-apps-script.js && ! grep -q 'НашСклад' apps/landing/google-apps-script.js && ! grep -q 'nashsklad' .claude/launch.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/landing/google-apps-script.js` line 2 is exactly ` * Оборот — Google Apps Script`
    - No occurrence of `НашСклад` remains in `apps/landing/google-apps-script.js`
    - `.claude/launch.json` contains `"oborot-landing"` (no `nashsklad-landing`)
    - Total line count of `apps/landing/google-apps-script.js` is unchanged (verify with `wc -l` before and after — same number)
  </acceptance_criteria>
  <done>
    Both files rebranded. No behavioral code changes (GAS script still deploys to the same Google Apps Script project — only header text changed).
  </done>
</task>

<task type="auto">
  <name>Task 3: Repo-wide grep verification and lockfile regeneration</name>
  <files>package-lock.json</files>
  <read_first>
    - .planning/phases/01-foundation-repo-cleanup/01-RESEARCH.md §Runtime State Inventory (full list of where nashsklad/НашСклад can occur; `.planning/**/*.md` is EXCLUDED — those are historical records per D-19)
    - .planning/phases/01-foundation-repo-cleanup/01-RESEARCH.md §Pitfall 7 (lockfile regen must be in the SAME commit as package.json rename so cache invalidates together)
    - .planning/phases/01-foundation-repo-cleanup/01-PATTERNS.md §1 gotcha (regenerate lockfile so CI cache hash flips)
  </read_first>
  <action>
    Step 1 — Verify no stray occurrences remain in CODE (excluding `.planning/`, `node_modules/`, `package-lock.json`):
    ```bash
    git grep -inE 'nashsklad|НашСклад' -- ':!.planning/' ':!package-lock.json' ':!node_modules/'
    ```
    Expected result: ZERO lines. If any match appears, update that file (extending the rename — per D-19 "любые другие вхождения"). Common expected zero: the planning docs in `.planning/` are intentionally excluded because they record history.

    Step 2 — Regenerate lockfile with new package names:
    ```bash
    rm -rf node_modules package-lock.json
    npm install
    ```
    This regenerates `package-lock.json` with `@oborot/landing` and `@oborot/app` as workspace entries (replacing the ~6 `@nashsklad/*` refs per RESEARCH §Runtime State Inventory). It also reinstalls all deps into `node_modules/` with corrected workspace symlinks.

    Step 3 — Verify lockfile correctness:
    ```bash
    grep -cE '@oborot/(landing|app)' package-lock.json  # expect ≥2
    ! grep -qE '@nashsklad/(landing|app)' package-lock.json  # expect 0
    ```

    Step 4 — Smoke build both apps to ensure nothing is broken by the rename:
    ```bash
    npm run build --workspace=apps/landing
    npm run build --workspace=apps/app
    ```
    The Next.js build (`apps/app`) will complain about missing `NEXT_PUBLIC_SUPABASE_URL` — that's expected at this stage since env validation (Plan 03) isn't wired yet. If it only complains about env and otherwise succeeds/produces `.next/`, that's acceptable for this task. If landing build fails (no env dependency), STOP and diagnose.

    Step 5 — Stage ALL changes from Tasks 1, 2, and 3 and commit as a single atomic rebrand:
    ```bash
    git add package.json apps/landing/package.json apps/app/package.json \
            apps/landing/google-apps-script.js .claude/launch.json package-lock.json
    git commit -m "chore: rebrand nashsklad to oborot"
    ```
    This single commit per D-18 covers: 3 package name renames + GAS header + launch.json + lockfile regen. Bisect-safe: every build step after this commit uses `@oborot/*` consistently.
  </action>
  <verify>
    <automated>git grep -iE 'nashsklad|НашСклад' -- ':!.planning/' ':!package-lock.json' | wc -l | xargs test 0 -eq && grep -qE '@oborot/landing' package-lock.json && grep -qE '@oborot/app' package-lock.json && ! grep -qE '@nashsklad/(landing|app)' package-lock.json && git log -1 --pretty=%s | grep -qE '^chore: rebrand nashsklad to oborot$'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -iE 'nashsklad|НашСклад' -- ':!.planning/' ':!package-lock.json'` returns zero lines
    - `package-lock.json` contains `@oborot/landing` and `@oborot/app` (at least one match each)
    - `package-lock.json` contains ZERO instances of `@nashsklad/` prefix
    - `npm run build --workspace=apps/landing` produces `apps/landing/dist/index.html`
    - Latest commit message is exactly `chore: rebrand nashsklad to oborot`
    - The commit touches exactly: 3 package.json files + google-apps-script.js + launch.json + package-lock.json (6 files)
    - `node_modules/@oborot/landing` symlink exists (workspace resolution works); `node_modules/@nashsklad` does NOT exist
  </acceptance_criteria>
  <done>
    Rebrand commit created. Grep clean across tracked code. Lockfile consistent. Both workspaces resolvable. Landing builds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm workspace symlinks | `node_modules/@nashsklad/*` must be purged, not left aliased |
| CI cache key | `package-lock.json` hash must change alongside package.json rename to invalidate stale CI caches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02-01 | Tampering | `package-lock.json` drift | mitigate | Regenerate lockfile in SAME commit as name rename (RESEARCH §Pitfall 7). Atomic rebrand keeps CI cache invalidation aligned. |
| T-1-02-02 | Denial of Service | Stale `node_modules/@nashsklad/*` symlinks breaking Wave 3+ installs | mitigate | Full `rm -rf node_modules package-lock.json && npm install` regenerates clean workspace tree. |
| T-1-02-03 | Information Disclosure | Secrets in `.claude/launch.json` | accept | File contains only debug config (program path, name). Verified no secrets present before modification. No mitigation needed beyond scope restriction. |
</threat_model>

<verification>
- `git log --oneline -1` shows `chore: rebrand nashsklad to oborot`
- `git grep` across tracked non-planning files finds zero brand strings
- Both workspaces build (landing fully; app builds until env validator step)
- `node_modules/@oborot/*` symlinks exist, `node_modules/@nashsklad/*` does not
</verification>

<success_criteria>
1. Single atomic commit `chore: rebrand nashsklad to oborot` covers all rename surfaces
2. All tracked code (`.js`, `.jsx`, `.json`, `.md` outside `.planning/`) free of old brand
3. `package-lock.json` fully regenerated, internally consistent with `@oborot/*`
4. Landing app still builds; app workspace resolution works for Wave 3 installs
5. `.planning/**/*.md` untouched (those are historical records per D-19)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-cleanup/01-02-SUMMARY.md` documenting:
- Which files were modified (the 6-file commit)
- Output of `git grep -i 'nashsklad|НашСклад' -- ':!.planning/' ':!package-lock.json'` (must be empty)
- `npm run build --workspace=apps/landing` tail output (built in ...)
- Any grep hits that required additional rename extensions beyond the CONTEXT matrix
</output>
