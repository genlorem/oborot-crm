---
phase: 01-foundation-repo-cleanup
plan: 01
subsystem: infra
tags: [gitignore, cleanup, monorepo-hygiene, dependency-pruning]

# Dependency graph
requires: []
provides:
  - ".gitignore covering .DS_Store (root + nested), Sentry build artifacts (.sentry/, .sentryclirc), coverage/, .vscode/, .idea/"
  - "Clean working tree — zero pre-monorepo root files, zero untracked .DS_Store"
  - "apps/landing deps without unused framer-motion (landing bundle 240.81 kB, -3 transitive packages)"
affects: [01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "gitignore-double-pattern: both .DS_Store and **/.DS_Store present for cross-implementation recursive matching"
    - "atomic-hygiene-sequence: gitignore-then-delete commit order so new patterns take effect before the cleanup commit"

key-files:
  created: []
  modified:
    - .gitignore
    - apps/landing/package.json
    - package-lock.json
  deleted:
    - .env.example, google-apps-script.js, index.html, postcss.config.js
    - tailwind.config.js, vite.config.js
    - src/App.jsx, src/main.jsx, src/index.css
    - src/components/{ChoiceCard,ProgressBar,RatingScale,SurveyStep,ThankYou}.jsx
    - src/data/questions.js, src/hooks/useSurvey.js
    - src/pages/{Landing,Survey}.jsx

key-decisions:
  - "Committed .gitignore BEFORE the 18-deletion commit so .DS_Store became ignored immediately, preventing accidental re-staging"
  - "Explicit per-file `git add -- <path>` for the 18 deletions instead of `git add -u` — excluded unrelated STATE.md modification from the cleanup commit"
  - "framer-motion safe to uninstall: zero import sites in apps/landing/src (verified via grep for 'framer-motion', 'motion/react', 'from \"framer'); post-uninstall vite build succeeded (240.81 kB, built in 484ms)"

patterns-established:
  - "Three-commit hygiene sequence: (1) extend .gitignore, (2) commit pre-staged deletions, (3) prune unused dep — each bisect-friendly"
  - "Scoped staging: explicit path list for deletion commits avoids pulling in unrelated tracked modifications"

requirements-completed: [FOUND-01]

# Metrics
duration: 2min
completed: 2026-04-21
---

# Phase 01 Plan 01: Working Tree Cleanup & .gitignore Hardening Summary

**Three atomic commits on main: extended .gitignore with .DS_Store + Sentry/coverage/IDE patterns, finalized the 18 pre-monorepo root-file deletions, and pruned unused framer-motion from apps/landing (landing build verified clean at 240.81 kB).**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-21T07:24:40Z
- **Completed:** 2026-04-21T07:26:28Z
- **Tasks:** 3
- **Files modified:** 3 (.gitignore, apps/landing/package.json, package-lock.json)
- **Files deleted:** 18 (pre-monorepo root legacy)

## Accomplishments

- **.gitignore now defensive:** `.DS_Store` + `**/.DS_Store` cover all git implementations; Sentry build artifacts (`.sentry/`, `.sentryclirc`) and test output (`coverage/`) pre-gated for upcoming FOUND-03 and FOUND-05; IDE dirs (`.vscode/`, `.idea/`) ignored.
- **Working tree legacy removed:** 18 pre-monorepo root files finalized in a single `chore: remove pre-monorepo root files` commit — `src/` gone from root index, `apps/` tree untouched.
- **framer-motion pruned:** removed from `apps/landing/package.json` + regenerated lockfile; landing build verified at 240.81 kB gzipped 77.46 kB (built in 484ms); 3 transitive packages dropped.
- **Bisect-friendly history:** each commit stands alone and reverts cleanly.

## Task Commits

Each task was committed atomically on `main`:

1. **Task 1: Extend .gitignore** — `6b43c4b` (chore)
2. **Task 2: Commit 18 pre-monorepo root-file deletions** — `5fd26b4` (chore)
3. **Task 3: Remove unused framer-motion from landing** — `63e1e6c` (chore)

**Plan metadata:** (will be created in final commit with SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md)

## Files Created/Modified

- **`.gitignore`** — extended with 4 new sections (OS metadata, Sentry artifacts, test output, IDE). Preserved the 6 existing lines.
- **`apps/landing/package.json`** — removed `"framer-motion": "^11.0.0"` from dependencies block (line 13 in old file).
- **`package-lock.json`** — regenerated by `npm uninstall --workspace=apps/landing`; 43 lines of framer-motion + transitive entries dropped.

**Deletions (18 files):**
- Root configs: `.env.example`, `google-apps-script.js`, `index.html`, `postcss.config.js`, `tailwind.config.js`, `vite.config.js`
- Root source: `src/App.jsx`, `src/main.jsx`, `src/index.css`
- Root components: `src/components/{ChoiceCard,ProgressBar,RatingScale,SurveyStep,ThankYou}.jsx`
- Root data/hooks/pages: `src/data/questions.js`, `src/hooks/useSurvey.js`, `src/pages/Landing.jsx`, `src/pages/Survey.jsx`

## Decisions Made

- **Commit order: gitignore → deletions → dep prune.** The plan required this bisect-friendly sequence. Committing `.gitignore` first means the `.DS_Store` patterns were active before any subsequent staging, preventing accidental re-addition.
- **Explicit per-file staging for the 18 deletions.** Used `git add -- <path1> <path2> ...` instead of `git add -u` because the working tree also contained an unrelated `M .planning/STATE.md` that belongs to the final plan-metadata commit (handled by the executor's state_updates step, not this task). Per user-level CLAUDE.md staging rule.
- **framer-motion pre-check:** grep for `framer-motion`, `'motion/react'`, `from "framer` in `apps/landing/src` returned zero matches → uninstall was safe. Post-uninstall `npm run build --workspace=apps/landing` succeeded with expected bundle size → no regression.

## Deviations from Plan

None — plan executed exactly as written.

All three task verification commands passed on first attempt:
- `.gitignore` contains all required patterns (`.DS_Store`, `**/.DS_Store`, `.sentry/`, `.sentryclirc`, `coverage/`, `.vscode/`, `.idea/`).
- `git status --porcelain | grep '^ D '` now returns zero lines.
- `grep framer-motion apps/landing/package.json` returns zero, grep of `src/` returns zero, `vite build` exits 0 with `built in 484ms`.

## Issues Encountered

- A PreToolUse hook on the initial `Write` to `.gitignore` briefly flagged a read-before-edit warning despite the file having already been read during the context-loading step of this execution. The Write had in fact succeeded; re-verification via grep confirmed content was correct and execution continued without re-writing the file.

## User Setup Required

None — no external service configuration required for this plan.

## Next Phase Readiness

- **Clean slate for the rest of Phase 1:** working tree is clean except for `.planning/STATE.md` (which the executor's state_updates step will handle), no stale root files, `.DS_Store` permanently ignored.
- **Ready for 01-02 (rebrand):** next plan can rename `nashsklad` → `oborot` across `package.json` / lockfile without being entangled with pre-existing deletions or untracked OS metadata.
- **Ready for 01-03 (FOUND-02 env validation):** `.env.example` at root is gone (correctly — it lived in the pre-monorepo root); plan 01-03 will create a fresh root `.env.example` as the single source of truth per D-21.
- **Ready for 01-04 (FOUND-03 tests):** `coverage/` already gitignored, so when Vitest starts emitting coverage it won't leak into commits.
- **Ready for 01-05 (FOUND-05 Sentry):** `.sentry/` and `.sentryclirc` already gitignored for build-time artifacts.
- No blockers or deferred items.

## Self-Check: PASSED

Verified before STATE.md updates:

- **File `.gitignore` exists + contents correct:** `grep '^\.DS_Store$' .gitignore` → matches; `grep '^\*\*/\.DS_Store$' .gitignore` → matches; `grep '^\.sentry/$' .gitignore` → matches; `grep '^coverage/$' .gitignore` → matches; `grep '^\.vscode/$' .gitignore` → matches; `grep '^\.idea/$' .gitignore` → matches.
- **Commit `6b43c4b` exists:** confirmed via `git log --oneline`.
- **Commit `5fd26b4` exists:** confirmed via `git log --oneline`; `git ls-files | grep '^src/' | wc -l` = 0 (root src/ tree fully removed).
- **Commit `63e1e6c` exists:** confirmed via `git log --oneline`; `grep framer-motion apps/landing/package.json` = 0 matches; `grep framer-motion package-lock.json` = 0 matches; `vite build` exits 0.
- **apps/app untouched:** `git diff HEAD~3 -- apps/app/` = 0 lines changed.
- **Zero ` D ` entries in `git status --porcelain`:** confirmed.

---
*Phase: 01-foundation-repo-cleanup*
*Completed: 2026-04-21*
